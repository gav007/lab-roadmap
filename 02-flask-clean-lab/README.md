# Lab 02: Clean Flask Setup

A clean Flask project using a Python virtual environment.

## Commands

```bash
python -m venv env
source env/Scripts/activate
pip install flask
pip freeze > requirements.txt
python src/app.py
